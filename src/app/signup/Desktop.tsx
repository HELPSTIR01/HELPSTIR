import { RegistrationForm } from "@/containers/Prejoining/SignupPage/RegistrationForm";
import { memo } from "react";
import "./desktop.scss";
export default memo(() => {
  return <RegistrationForm mode="desktop" />;
});
